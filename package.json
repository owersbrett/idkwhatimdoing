{
  "name": "idkwhatimdoing",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Date-indexed Q&A archive + concept lexicon for self-taught devs learning to work with Claude. `npm run dev` serves on http://localhost:4747",
  "scripts": {
    "db:build": "node scripts/build-db.mjs",
    "dev": "npm run db:build && vite",
    "build": "npm run db:build && vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.40.0",
    "@types/better-sqlite3": "^7.6.12",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "better-sqlite3": "^12.0.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.0",
    "wrangler": "^4.98.0"
  }
}
